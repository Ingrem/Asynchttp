ƒобрый день, јлексей,

ќчень радует что у ¬ас хороший прогресс по задаче. 
Ќо есть несколько серьезных замечаний. 

1. ќтлично что реализовали консольный http сервис. Ќо задача подразумевала использование паттерна MVC и IIS сервера дл€ работы с http запросами. 
	Ёто нужно будет переделать.
2. ¬ задание ничего не сказано про клиентские приложени€ дл€ отправки http запросов.
	≈сли ¬ам удобно использовать собственные http клиенты дл€ тестировани€ - отлично.
	Ќо провер€ть работоспособность сервисов мы будем с помощью любого REST клиента.
	Ќапример, можно использовать плагин дл€ хрома Advanced REST Client.
	ќсновна€ разница в том что в REST клиентах намного проще смотреть все содержимое http запроса.  
	“ак же при таком подходе ¬ам не нужно тратить дополнительное врем€ на разработку собственных http клиентов. 
3. ¬се конфигурационные параметры(http адреса, порты, логины и т.д) должны быть вынесены в config файл. 
4. ƒл€ RabbitMQ нужно будет создать пользовател€ и создавать соединени€ с логином/паролем, вместо использовани€ пользовател€ по умолчанию.

«амечани€ по функциональности пока не пишу, потому что на изменени€ выше ¬ам потребуетс€ какое-то врем€. 